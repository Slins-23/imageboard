apiVersion: v1
kind: PersistentVolume
metadata:
  name: frontend-dev-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: frontend-dev-data
  hostPath:
    path: /mnt/frontend-dev
