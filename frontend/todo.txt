make all versions of a compontent
(e.g. all buttons)


make sure components and website behaves similarly across different browsers

where to use rem vs em fonts

replace == for ===, etc.

export default vs export
for component functions and/or types, differences

▼

replace forEach by for...of


why is there extra width on the list item to the right when opened

how to properly resize the dropdown with window resizes, zooms, font changes

add extra padding to the arrow (largest item is too close)

how to remove selected item focus from hover (works fine on keyboard, but can't remove classes from hovering)

why is there an undefined class in dropdown menu <UL>

why border still appears when closing?

why behaves weird changes border extent on resize

border weird because of apparent rounding issues? only one pixel extra or one pixel less, deterministic and predictable

understand useLayoutEffect, useEffect more react tutorial crash course