apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-dev
  template:
    metadata:
      labels:
        app: frontend-dev
    spec:
      containers:
        - name: frontend-dev
          image: node:24.15.0-bookworm
          ports:
            - containerPort: 3000 # NextJS
            - containerPort: 8080 # Storybook
          command: ["sh", "-c"]
          args:
            - cd /app && npm install && npx concurrently "npm run dev" "npm run storybook"
